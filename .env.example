# Azure OpenAI Variables
# For the AZURE_RESOURCE_NAME this is the unique component of your AZURE ENDPOINT
# For example, for an AZURE_ENDPOINT like this: https://example-organisation-us-east.openai.azure.com/
# The AZURE_RESOURCE_NAME would be example-organisation-us-east
AZURE_OPENAI_API_KEY=
AZURE_RESOURCE_NAME=
AZURE_OPENAI_EMBEDDING_MODEL=text-embedding-3-large
OPENAI_API_VERSION=2024-12-01-preview



# Elasticsearch configuration
ELASTIC_MODE=local
# ELASTIC_MODE=cloud  # Uncomment to use Elastic Cloud instead of local

# Local Elasticsearch settings (used when ELASTIC_MODE=local)
ELASTIC_HOST=http://localhost:9200

# Elastic Cloud settings (used when ELASTIC_MODE=cloud)
# ELASTIC_CLOUD_ID=your-deployment-id:region
# ELASTIC_API_KEY=your-api-key
# Or use username/password instead of API key
# ELASTIC_USERNAME=elastic
# ELASTIC_PASSWORD=your-password

# Elasticsearch logging configuration
ENVIRONMENT=localhost  # Change to dev, preprod, prod in respective environments
ELASTIC_LOGS_INDEX_FRONTEND=logs-frontend
ELASTIC_LOGS_INDEX_PIPELINE=logs-pipeline

LEX_API_KEY=token-abcd1234